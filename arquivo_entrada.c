#include "bib.h"
#define DEZ 10
#define QUINZE NUM
#define PLAY PS

// Trabalho de Igor de Paula e Fernando Castilho
/*
    Isso aqui vai ser apagado
           (talvez)

*/

int main(){
    char console[QUINZE] = PLAY;
    int x = soma(DEZ,QUINZE);//O que vem antes não(int x = soma(DEZ,QUINZE);) vai sair, mas esse aqui sai.
    printf("x = %d\n",x);
    printf("%d\n",QUINZE);
    printf("%s\n",console);

    return 0;
}